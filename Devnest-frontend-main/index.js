function clone(arr){
    var brr = arr;
    console.log("The cloned array is:" ,brr)
    return 0;
}

function firstn(arr, n){
    var len arr.length;
    for(let i = 0;i<n;i++) {
        console.log(arr[i]);
    }
    console.log("the length of the array is:",len);
}

firstn([1,2,3,4,5,6], 3);

function joinstr(arr) {
    lstr=arr.join("")
    console.log(str)
    }

    joinstr(["Hello", "world", "this" , "javascript"]);
}